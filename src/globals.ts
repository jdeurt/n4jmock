export const sourceCode = {
    files: {} as Record<string, string>,
};

export const options = {
    verbose: false,
    nodes: 1,
    relationships: 1,
    out: "./query.cypher",
    noFile: false,
};
